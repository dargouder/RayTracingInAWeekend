cmake_minimum_required(VERSION 2.6)
project (Tutorial)


#include headers
include_directories(include)

#manually add the sources using the set command
#set(SOURCES src/Student.cpp tutorial.cpp)

file(GLOB SOURCES "src/*.cpp tutorial.cpp")

add_executable(Tutorial ${SOURCES})
